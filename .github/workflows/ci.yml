name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"
  RUSTDOCFLAGS: "-D warnings"
  # The plan harnesses write no bytecode into the tree that they verify.
  PYTHONDONTWRITEBYTECODE: "1"

jobs:
  dod:
    name: Definition of Done (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [macos-26, ubuntu-26.04]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: rustfmt, clippy
          cache-save-if: ${{ github.ref == 'refs/heads/main' }}
      - name: Linux system dependencies for GPUI and for the audio stack
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libxkbcommon-dev libxkbcommon-x11-dev libwayland-dev libx11-xcb-dev \
            libxcb1-dev libxcb-shape0-dev libxcb-xfixes0-dev libasound2-dev \
            libpipewire-0.3-dev libfontconfig1-dev libfreetype6-dev libssl-dev \
            libgit2-dev libvulkan-dev mesa-vulkan-drivers pkg-config cmake clang \
            mold shellcheck
      - name: macOS shell lint dependency
        if: runner.os == 'macOS'
        run: brew install shellcheck
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-deny,cargo-machete,cargo-nextest@0.9.145,typos-cli
      - name: Definition of Done
        run: scripts/dod.sh
        env:
          NEXTEST_PROFILE: ci

  changes:
    name: Changed paths
    runs-on: ubuntu-26.04
    outputs:
      roadmap: ${{ steps.filter.outputs.roadmap }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Detect a change to the plan or to its guards
        id: filter
        run: |
          # Each diff prints one path per line, whatever bytes the path holds.
          # `-z` prints each path RAW and NUL separated, so nothing is quoted
          # and no byte is escaped; a quoted path fails the anchored pattern
          # below and this job would answer `false` for a plan change it never
          # saw. `--no-renames` prints BOTH sides of a rename, so a document
          # moved OUT of `roadmap/` is still seen. The same form runs in
          # `scripts/dod.sh`.
          base="${{ github.base_ref }}"
          diffnames() { git -c core.quotePath=false diff --no-renames -z --name-only "$@" | tr '\0' '\n'; }
          if [ -n "$base" ]; then
            changed="$(diffnames "origin/$base"...HEAD)"
          elif git rev-parse --verify --quiet HEAD^ >/dev/null; then
            changed="$(diffnames HEAD^ HEAD)"
          else
            changed="roadmap/"
          fi
          if printf '%s\n' "$changed" | grep -qE '^roadmap/|^tools/bc_repo_guard/xtask/'; then
            echo "roadmap=true" >> "$GITHUB_OUTPUT"
          else
            echo "roadmap=false" >> "$GITHUB_OUTPUT"
          fi

  plan-lint:
    name: Plan guards
    needs: changes
    if: needs.changes.outputs.roadmap == 'true'
    runs-on: ubuntu-26.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: rustfmt, clippy
          cache-save-if: ${{ github.ref == 'refs/heads/main' }}
      - name: Linux system dependencies for the roster compile
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libxkbcommon-dev libxkbcommon-x11-dev libwayland-dev libx11-xcb-dev \
            libxcb1-dev libxcb-shape0-dev libxcb-xfixes0-dev libasound2-dev \
            libpipewire-0.3-dev libfontconfig1-dev libfreetype6-dev libssl-dev \
            libgit2-dev libvulkan-dev mesa-vulkan-drivers pkg-config cmake clang \
            mold
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-nextest@0.9.145
      - name: Build the plan store binary
        run: cargo build --release -p plan-db
      - name: Placement
        run: cargo xtask check-placement roadmap/duet-v1/architecture.md
      - name: Plan graph
        run: cargo xtask check-plan-graph roadmap/duet-v1
      - name: Plan manifest drift
        run: cargo xtask check-plan-graph roadmap/duet-v1 --check-manifest
      - name: Roster compile
        run: cargo xtask check-roster roadmap/duet-v1/architecture.md "$RUNNER_TEMP/roster" .
      - name: Reason texts
        run: cargo xtask check-conversions --appendix roadmap/duet-v1/architecture.md
      - name: Probes
        run: cargo nextest run -p xtask --test probes --no-tests=fail
