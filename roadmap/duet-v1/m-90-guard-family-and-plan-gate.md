---
id: M90
line: M
depends_on: [M0, T1]
write_scope:
  - tools/bc_repo_guard/xtask/lang_rust/src/main.rs
  - tools/bc_repo_guard/xtask/lang_rust/src/check_plan_graph.rs
  - tools/bc_repo_guard/xtask/lang_rust/src/check_placement.rs
  - tools/bc_repo_guard/xtask/lang_rust/src/check_roster.rs
  - tools/bc_repo_guard/xtask/lang_rust/src/check_conversions.rs
  - tools/bc_repo_guard/xtask/lang_rust/src/check_closure.rs
  - tools/bc_repo_guard/xtask/lang_rust/tests/probes.rs
  - scripts/dod.sh
  - typos.toml
  - .github/workflows/ci.yml
  - roadmap/duet-v1/tools/conversion_check.py
  - roadmap/duet-v1/architecture.md
  - roadmap/duet-v1/plan-graph.md
parallelism: serial-only: SM4 makes `scripts/dod.sh`, `typos.toml`, and `.github/workflows/ci.yml` policy files, so the Orchestrator executes this chunk and no engineer runs beside it in the same files.
completion: "bash scripts/dod.sh --plan lists the plan line and prints no advisory word beside typos; cargo nextest run -p xtask --test probes --no-tests=fail passes; typos exits 0; cargo xtask check-conversions --appendix roadmap/duet-v1/architecture.md exits 0; cargo xtask check-placement roadmap/duet-v1/architecture.md exits 0; cargo xtask check-plan-graph roadmap/duet-v1 --check-manifest exits 0; commit SHA on a branch chunk/m90-guard-family-and-plan-gate"
---

# M90: The guard message vocabulary, the roster split, the typos configuration, and the plan-guard gate step

Verify the current state of the files in the write scope; report a discrepancy and stop, instead of
proceeding.

This is a REPAIR chunk under rule SM9 of architecture section 13.0. It lands the code half of six
escalations that chunks M0 and T1 opened on 2026-09-22: M0-1, M0-2, M0-4, M0-5, T1-5, and the
guard half of T1-1. It implements architecture section 1.9 (the guard vocabulary block and the
`--generate-only` block), section 2.3 rule CG9, section 1.5 rule CL1d, section 13.0 rules SM1, SM4
and SM9, section 14 rung one (the gate definition, the path-conditional plan guards, the `typos`
bullet, and the `plan-lint` rows), ADR-0009 decisions 2 and 3, and ADR-0010 in full.

**This chunk runs AFTER chunk M91 and before every line chunk of phase 1.** Section 13.3 prints the
order and section 13.0 rule SM1 states the rule. Step 20 below adds rule CG9, which compares each
Appendix B.1 reason cell with the `reason =` string of `crates/bc_time/duet-time/lang_rust/src/convert.rs`, and M91 is
the chunk that repairs those strings. **If `crates/bc_time/duet-time/lang_rust/src/convert.rs` still holds the scale
factor 2^23 - 1 when this chunk starts, M91 has not landed: report it and stop.**

**The Orchestrator executes this chunk directly.** `scripts/dod.sh`, `typos.toml`, and
`.github/workflows/ci.yml` are policy files under SM4, and CLAUDE.md makes a gate edit an
adjudication. No engineer may edit one.

**This chunk writes THREE paths under `roadmap/`, and SM9 rules 2 and 4 are the authority.** The
three are `roadmap/duet-v1/plan-graph.md`, `roadmap/duet-v1/architecture.md`, and
`roadmap/duet-v1/tools/conversion_check.py`, and the write scope above holds exactly those three.
`roadmap/duet-v1/plan-graph.md` is generated by `cargo xtask check-plan-graph roadmap/duet-v1
--write-manifest`; step 2 changes the generator, so this chunk regenerates the file in the same
commit (rule 2). `roadmap/duet-v1/architecture.md` and `roadmap/duet-v1/tools/conversion_check.py`
take the registration sub-items b, c, d and e of step 21 alone (rule 4): the `rows>=` marker, the
guard-block row, the two section 1.7 index cells, and the prototype rule token are each refused on
their own by PG27, PG27b, PG29 or PG39, and they pass only in the commit that also lands sub-item a,
which bumps the `DATA_BLOCKS` register of `tools/bc_repo_guard/xtask/lang_rust/src/check_placement.rs`. **The Architect
wrote every one of those texts; step 21 quotes them character for character.** This chunk edits no
chunk brief and no other word of `architecture.md`.

## Files

| Path | Action |
|---|---|
| `tools/bc_repo_guard/xtask/lang_rust/src/check_plan_graph.rs` | modify (`MANIFEST_NOTE` and the two `collect_chunks` paths) |
| `tools/bc_repo_guard/xtask/lang_rust/src/check_placement.rs` | modify (the empty-candidate path of `run`) |
| `tools/bc_repo_guard/xtask/lang_rust/src/check_roster.rs` | modify (three verdict lines and the `--generate-only` half) |
| `tools/bc_repo_guard/xtask/lang_rust/src/check_conversions.rs` | modify (rule CG9) |
| `tools/bc_repo_guard/xtask/lang_rust/src/check_closure.rs` | modify (rule CL1d) |
| `tools/bc_repo_guard/xtask/lang_rust/src/main.rs` | modify (the `CheckRoster` flag and the `CheckConversions` `--appendix` argument) |
| `tools/bc_repo_guard/xtask/lang_rust/tests/probes.rs` | modify (three vocabulary probes, the roster probe group, the job-line probe, the CG9 probes, the CL1d probes) |
| `scripts/dod.sh` | modify (the `typos` step, the new plan step, and the `--appendix` condition on the `converts` step) |
| `typos.toml` | **modify.** The file EXISTS at the repository root today, at 244 bytes: TWO comment lines, a `[files] extend-exclude` list of four entries, and a `[default.extend-words]` table that holds `gpui` and `heed`. Step 13 replaces the whole content and keeps both of those entries |
| `.github/workflows/ci.yml` | modify (the `plan-lint` job) |
| `roadmap/duet-v1/tools/conversion_check.py` | modify, by the exact sub-item e of step 21 alone (the `CG9` token PG29 reads) |
| `roadmap/duet-v1/architecture.md` | modify, by the exact sub-items b, c and d of step 21 alone (SM9 rule 4) |
| `roadmap/duet-v1/plan-graph.md` | modify, by regeneration only (SM9 rule 2) |

## Types and signatures

The chunk adds one field to the roster command of `tools/bc_repo_guard/xtask/lang_rust/src/main.rs`.

```rust
    /// Compile the section 15 roster in a scratch workspace outside the
    /// repository.
    CheckRoster {
        /// The architecture document to read.
        document: PathBuf,
        /// A scratch directory outside the repository.
        scratch: PathBuf,
        /// The repository root.
        repo: PathBuf,
        /// Write the scratch workspace and skip every cargo command.
        #[arg(long)]
        generate_only: bool,
    },
```

`check_roster::run` takes the flag as its fourth parameter.

```rust
pub(crate) fn run(
    document: &Path,
    scratch: &Path,
    repo: &Path,
    generate_only: bool,
) -> anyhow::Result<Outcome>;
```

Every other signature this chunk touches keeps its current shape. `Outcome` stays the three-arm enum
of `tools/bc_repo_guard/xtask/lang_rust/src/main.rs`.

## Steps

1. Read `tools/bc_repo_guard/xtask/lang_rust/src/check_plan_graph.rs`, `tools/bc_repo_guard/xtask/lang_rust/src/check_placement.rs`,
   `tools/bc_repo_guard/xtask/lang_rust/src/check_roster.rs`, `tools/bc_repo_guard/xtask/lang_rust/src/check_conversions.rs`,
   `tools/bc_repo_guard/xtask/lang_rust/src/check_closure.rs`, `scripts/dod.sh`, `typos.toml`,
   `.github/workflows/ci.yml`, `roadmap/duet-v1/tools/conversion_check.py`, and
   `crates/bc_time/duet-time/lang_rust/src/convert.rs`. Confirm that each file holds the state this chunk describes.
   **Confirm in particular that `crates/bc_time/duet-time/lang_rust/src/convert.rs` holds the scale factor
   `8_388_608.0` for `UNIT_TO_I24_SCALE`**, which means chunk M91 has landed. Report a discrepancy
   and stop.

2. Repair `MANIFEST_NOTE` in `tools/bc_repo_guard/xtask/lang_rust/src/check_plan_graph.rs`. It still credits the Python
   prototype, which no longer generates the file (chunk M0 report, R3-N3). The new value is exactly:

   ```rust
   /// The line that tells a reader `plan-graph.md` is generated.
   const MANIFEST_NOTE: &str = "Derived from the chunk front-matter by `cargo xtask check-plan-graph <plan-dir> --write-manifest`. Edit the chunk files, then regenerate; never edit this file by hand.";
   ```

3. Write the failing probe for the duplicate chunk id. Add it to the plan-graph group of
   `tools/bc_repo_guard/xtask/lang_rust/tests/probes.rs`, beside `plan_graph_unknown_dependency_is_a_finding`. It builds two
   chunk files that state one id, runs the guard, and asserts exit 1 and a report line that opens
   `FINDING: duplicate chunk id`. Name it
   `plan_graph_vocabulary_duplicate_chunk_id_is_a_finding`. Run
   `cargo nextest run -p xtask --test probes -E 'test(vocabulary)' --no-tests=fail` and confirm that
   it fails.

4. Repair the duplicate-id path of `collect_chunks` in `tools/bc_repo_guard/xtask/lang_rust/src/check_plan_graph.rs`. The
   guard prints `FINDING: duplicate chunk id {id} in {name} and {first}` and returns
   `Outcome::Findings`. The exit code does not change; only the word does. Architecture section 1.9
   states the decision: rule 1 of this guard is that every id is unique, so two files that state one
   id break a rule the guard decided. Run the probe and confirm that it passes.

5. Write the failing probe for the empty plan directory. It builds a plan directory that holds
   `architecture.md` and no chunk file, runs the guard, and asserts exit 2 and a report line that
   opens `FAIL: no chunk file found`. Name it
   `plan_graph_vocabulary_empty_directory_fails_closed`. Run the filter and confirm that it fails.

6. Repair the empty-directory path of `collect_chunks`. The guard prints
   `FAIL: no chunk file found; the guard is fail-closed.` and returns `Outcome::FailClosed`. It is a
   zero denominator, and rule CG1b part 1 of architecture section 2.3 already gives that answer for
   a member set. Run the probe and confirm that it passes.

7. Write the failing probe for the empty candidate set. It runs `check-placement` over a synthetic
   document that holds no declaration the parser recognises, and it asserts exit 2 and the line
   `FAIL: the candidate set is empty, so the parse is broken.` Name it
   `placement_vocabulary_empty_candidate_set_fails_closed`. Build the fixture the way
   `placement_run` already does, so the probe reads no file of this repository. Run the filter and
   confirm that it fails.

8. Repair the empty-candidate path of `run` in `tools/bc_repo_guard/xtask/lang_rust/src/check_placement.rs`. It returns
   `Outcome::FailClosed` and keeps its line unchanged. The line says the PARSE is broken, so the run
   measured nothing about the document and exit 1 would report a breach it never made. Run the probe
   and confirm that it passes.

9. Repair the three verdict lines of `report` in `tools/bc_repo_guard/xtask/lang_rust/src/check_roster.rs`. Each one keeps
   its text after the first word and returns `Outcome::Findings` as it does today. The three are the
   roster below the section 1.5 denominator, the mixed impl block, and the impl count that differs
   from the `impl-sites` block.

   ```rust
            "FINDING: the roster holds {} items and section 1.5 places {floor}; the first name with no declaration is {}.",
   ```

   ```rust
                "FINDING: the impl block for {} carries a body beside a bodiless signature, so the whole block leaves the roster; write every item as a signature or write every item with a body.",
   ```

   ```rust
            "FINDING: the roster parsed {} impl blocks and the `impl-sites` block lists {impl_floor}; the two are one set and they differ by {}.",
   ```

   Architecture section 1.9 records all three, in the PG25 row, with these exact texts. A text that
   differs from the row is a plan defect that returns to the Architect.

10. Add the `--generate-only` flag to `Command::CheckRoster` in `tools/bc_repo_guard/xtask/lang_rust/src/main.rs` and pass
    it to `check_roster::run`. The signature block above states the shape.

11. Implement the flag in `tools/bc_repo_guard/xtask/lang_rust/src/check_roster.rs`. With the flag set the guard writes the
    scratch workspace, runs no cargo command, prints every `ROSTER ...` counter line it prints
    today, then prints exactly `ROSTER COMPILE:   skipped (--generate-only)`, and returns the
    outcome the parse rules and the count rules decide. Without the flag the guard behaves exactly
    as it does today and prints no such line. Architecture section 1.9 holds the four-part contract,
    and part 4 of it is the safety argument: a skipped run prints a line no full run prints.

12. Write the roster probe group in `tools/bc_repo_guard/xtask/lang_rust/tests/probes.rs`. Each test spawns
    `cargo xtask check-roster <document> <scratch> <repo> --generate-only` over a synthetic
    document in a scratch directory the test owns, exactly as `placement_run` does, so no probe
    reads this repository. Name the group with the prefix `roster_generate_only_`. Section 1.9
    part 3 names the three cheap shapes it holds.

13. Repair `typos.toml`. **The file exists and this step REPLACES its content**; it does not create
    it. The whole file becomes:

    ```toml
    # typos-cli configuration. scripts/dod.sh runs a plain `typos` call, so a
    # finding fails the gate.
    [files]
    extend-exclude = [
      "target/",
      "Cargo.lock",
      ".hypervisor/",
      "*.svg",
      "crates/bc_app/duet/assets/fonts/bravura_metadata.json",
      "roadmap/duet-v1/reviews/",
      "roadmap/duet-v1/research/",
    ]

    [default.extend-words]
    gpui = "gpui"
    heed = "heed"
    Criticals = "Criticals"
    criticals = "criticals"
    CRITICALS = "CRITICALS"
    tuplets = "tuplets"

    [default.extend-identifiers]
    note32ndUp = "note32ndUp"
    ArticAccent = "ArticAccent"
    ArticStaccato = "ArticStaccato"
    ```

    **The glyph-name prefix takes two IDENTIFIER entries and no word entry** (critic C1-12). A word
    entry for the prefix alone would hide a real misspelling of the word "arctic" anywhere in the
    repository for ever, and the path exclusion above already answers every hit of that prefix
    inside the font file. The two identifier entries answer every hit that remains, because each
    one is a whole SMuFL glyph name. Architecture section 14 rung one states the whole partition,
    the FIVE classes, the rule for each class, and the rule's own limit. **Run `typos` and confirm
    that it exits 0 and prints nothing. That is the one measurement this step reports**; do not
    report a finding count.

    **A document that quotes a misspelling creates a finding of its own.** `typos` reads
    `architecture.md`, every ADR, and this chunk file, so architecture section 14 and ADR-0010 name
    each accepted typo by its CORRECTION and by its file and line, and never by its misspelled
    token. Keep that rule in any later edit of either text. **The same self-reference is why no
    document of this plan states a plain-`typos` TOTAL** (critic C1-3, C2-6): the sentence that
    states the partition writes the very tokens it counts into three more checked files, so each
    re-measurement refutes the number the last one wrote. The class set is the stable statement and
    the exit code is the stable measurement.

14. Repair the `typos` step of `scripts/dod.sh`. Delete the `--exclude` flag, delete the
    `|| printf ...` fallback, and delete the paragraph of comment that explains the fallback. The
    step becomes a plain call that fails the gate on a finding. Update the `GATES` array entry from
    `"typos      typos (advisory; when installed)"` to `"typos      typos (when installed)"`.

15. Add the path-conditional plan-guard step to `scripts/dod.sh`, after the `manifests` step and
    before the `hooks` step. Add `"plan       cargo xtask check-placement and check-plan-graph (when the change touches roadmap/)"` to the `GATES` array. The step runs these three commands, in
    this order, and only when the change under test names a path that opens `roadmap/`:

    ```
    cargo xtask check-placement roadmap/duet-v1/architecture.md
    cargo xtask check-plan-graph roadmap/duet-v1
    cargo xtask check-plan-graph roadmap/duet-v1 --check-manifest
    ```

    **The denominator has THREE clauses** (critic C1-2). A two-clause denominator is EMPTY at every
    push and on every amend, so the step would always skip and always report a pass it did not earn.
    `.githooks/pre-push` runs `scripts/dod.sh` over a clean tree, and `git commit --amend` diffs
    against the commit it replaces. The union is:

    ```
    git diff --cached --name-only
    git diff --name-only HEAD
    git diff --name-only @{upstream}..HEAD
    ```

    **When the branch has no upstream**, the third clause becomes
    `git diff --name-only "$(git merge-base origin/main HEAD)"..HEAD`. **When neither `@{upstream}`
    nor `origin/main` resolves, the step RUNS.** A step that cannot compute its denominator does not
    skip: that is the fail-closed answer the guard family applies everywhere else, and the three
    commands cost about one second between them. When the union names no path that opens `roadmap/`,
    the step prints `plan guards: skipped (no change under roadmap/)` and the script continues. A
    skip PRINTS; it is never silent. Architecture section 14 rung one states the two residual limits
    that remain after the third clause.

    **Compute the union ONCE, into one shell variable, and read it TWICE** (critic C2-1). The
    `plan` step reads it for a path that opens `roadmap/`. The `converts` step reads the same
    variable for rule CG9, under the wider condition of step 15a. Do not compute a second
    denominator; one union, two conditions.

    **Compute the union ABOVE the first `step` call of the script, and never inside a gate step.**
    The live `GATES` order runs `converts` BEFORE `manifests`, and the `plan` step goes after
    `manifests`, so the FIRST reader of the variable is `converts` and not `plan`. A script that
    computes the union at the `plan` step leaves `converts` with an unset variable. `set -u` makes
    that loud, and the two runs below catch it, so this sentence saves one wasted attempt.

    **Verify the third clause before you go on.** On a branch that holds one commit that touches
    `roadmap/`, with a clean tree, run `bash scripts/dod.sh` and confirm that the step RUNS and
    prints no skip line. Then run it on a branch whose commits touch no `roadmap/` path and confirm
    that it prints the skip line. Record both observations in the chunk report.

15a. Make the `converts` step of `scripts/dod.sh` PATH CONDITIONAL for rule CG9 alone (critic
    C2-1). The `converts` step keeps its place and keeps running on every commit, because CG1 to CG8
    read crate sources and bind nothing in a plan document. It gains one argument under one
    condition. **When the union of step 15 names a path that opens `roadmap/`, or names
    `crates/bc_time/duet-time/lang_rust/src/convert.rs`, the step runs**

    ```
    cargo xtask check-conversions --appendix roadmap/duet-v1/architecture.md
    ```

    **Otherwise it runs `cargo xtask check-conversions` with no argument**, and the guard prints
    `REASON TEXTS:    skipped (no --appendix)`. Update the `GATES` array entry from
    `"converts   cargo xtask check-conversions"` to
    `"converts   cargo xtask check-conversions (CG9 when the change touches roadmap/ or convert.rs)"`.

    **Both halves of the binding are in the condition and one half alone would be wrong.** A
    `roadmap/`-only condition would skip CG9 on the commit that edits the code half, and that is
    exactly a commit that can break the binding. Architecture section 2.3 rule CG9 part 6 states the
    rule, what the condition covers, what it does not, and the party that clears a red CG9.

    **Verify both arms.** On a branch whose only commit touches `crates/bc_time/duet-time/lang_rust/src/convert.rs`,
    run `bash scripts/dod.sh` and confirm that the `converts` step prints the CG9 counter and no
    skip line. On a branch whose commits touch neither half, confirm that it prints
    `REASON TEXTS:    skipped (no --appendix)`. Record both observations in the chunk report.

16. Amend the `plan-lint` job of `.github/workflows/ci.yml`. Its `changes` filter already matches
    `roadmap/` and `tools/bc_repo_guard/xtask/lang_rust/`. Add the two `check-plan-graph` lines after the `check-placement`
    line. Add the line
    `cargo xtask check-conversions --appendix roadmap/duet-v1/architecture.md`, so the job runs rule
    CG9 always and never under a condition (critic C2-1). **Do not widen the `changes` filter.** A
    pull request that touches `crates/bc_time/duet-time/lang_rust/src/convert.rs` and no `roadmap/` or `tools/bc_repo_guard/xtask/lang_rust/`
    path therefore runs no CG9 at the merge; the local step of step 15a is the cover, and
    architecture section 2.3 rule CG9 part 6 states that gap and its reason, which is the 4.0 GB
    roster compile this job carries. Leave the `check-roster` line with no `--generate-only` flag, because this job is the one
    reading that compiles. Add no `check-closure` line: architecture section 14 and rule CL1c both
    state that the guard runs at review time on the machine that holds the plan store.

17. Regenerate the plan manifest. Run `cargo xtask check-plan-graph roadmap/duet-v1
    --write-manifest`, then READ the first line of `roadmap/duet-v1/plan-graph.md` after the title
    and confirm that it cites `cargo xtask check-plan-graph` and no Python path. **The human read is
    the proof of the text** (critic C1-16). Run
    `cargo xtask check-plan-graph roadmap/duet-v1 --check-manifest` and confirm exit 0; that exit
    code proves CONSISTENCY alone. The generator writes `MANIFEST_NOTE` and `--check-manifest`
    compares the file against the same generator, so it exits 0 for any value of that constant and
    it proves nothing about the repair. Quote the read line in the chunk report.

18. Write the probe that keeps `--generate-only` out of every job (critic C1-8, C2-7). Add it to
    `tools/bc_repo_guard/xtask/lang_rust/tests/probes.rs`. Write one private helper in the test module,
    `fn roster_job_line_is_clean(workflow: &str) -> bool`, which returns false when any line of the
    text names `check-roster` and also names `--generate-only`. Then write TWO tests.

    The first, `roster_generate_only_never_reaches_a_job`, reads **EVERY file under
    `.github/workflows/`** and asserts that the helper returns true for each one. Resolve the
    directory from `env!("CARGO_MANIFEST_DIR")` and read it with `std::fs::read_dir`; do not read
    one path through `include_str!`. **The test is fail-closed on its own input**: a directory that
    does not open is a failure, and a directory that yields zero files is a failure, because a
    denominator of zero is the vacuous green CG1b refuses. The assert message names the file that
    failed.

    The second, `roster_job_line_probe_refuses_a_planted_flag`, passes a two-line fixture that
    carries the flag and asserts that the helper returns false. **The second test is the positive
    control and it is committed**, so no reader has to plant a defect by hand to know the probe can
    go red. Run both and confirm that both pass.

    **Why the denominator is the directory and not one file.** The rule is headed "every job". A
    second workflow that runs the roster is unguarded from the day it is written, and this plan adds
    `soak.yml` and `audio-smoke.yml`. The directory holds `ci.yml` alone today, so the wider
    denominator costs one directory read and changes no result now. Architecture section 1.9 part 3
    states the rule's own limit: the probe reads one LINE at a time, so a folded YAML scalar, a
    shell variable, and an `env:` entry each defeat it, and review holds those.

19. Write the failing probes for rule CG9 (critic C1-4, C2-4). Add them to
    `tools/bc_repo_guard/xtask/lang_rust/tests/probes.rs` in the conversion group. Each one builds a throwaway cargo
    workspace in a scratch directory the test owns, with one member whose `src/convert.rs` holds an
    `#[expect]` attribute and a `fn`, and with a synthetic `roadmap/duet-v1/architecture.md` under
    the scratch root that carries a `b1-convert` guard block. Each one passes
    `--appendix <scratch root>/roadmap/duet-v1/architecture.md`. **No CG9 test reads this
    repository.** **CP9 holds FIVE shapes: four that fail and one green control.** The four failing
    shapes are:

    - `conversions_reason_text_that_differs_is_a_finding`, where the cell and the string differ by
      one character;
    - `conversions_reason_row_with_no_site_is_a_finding`, where the block names a function the file
      does not declare;
    - `conversions_reason_site_with_no_row_is_a_finding`, where the file gives a reason that the
      block does not name. Each of these three asserts exit 1 and one line that opens
      `  REASON TEXT: `;
    - `conversions_appendix_that_does_not_open_fails_closed`, where the scratch root holds NO
      `roadmap/duet-v1/architecture.md` and the `--appendix` argument names that absent path. It
      asserts **exit 2** and the one named line CG9 part 1 mandates.

    Add `conversions_reason_text_that_matches_is_clean` as the green control. Run the group and
    confirm that the four fail and the control passes.

    **The fourth shape is the one this probe set may not skip** (critic C2-4). The absent-document
    path is the one path where a WRONG implementation passes instead of failing: an implementation
    that reads a missing document as "no rows, nothing to compare" exits 0, and the other four
    probes stay green. That is a vacuous green, which this document refuses in its own words. PG2
    plants a path that does not exist and CG8 plants a workspace `cargo metadata` refuses; CG9
    plants the same class of shape.

20. Implement rule CG9 in `tools/bc_repo_guard/xtask/lang_rust/src/check_conversions.rs`, and add the `--appendix <path>`
    argument to the `CheckConversions` command in `tools/bc_repo_guard/xtask/lang_rust/src/main.rs`. Architecture section
    2.3 states the SIX parts and this chunk adds no part of its own. **The rule runs only when the
    caller passes `--appendix <path>`**; with no such argument the guard prints
    `REASON TEXTS:    skipped (no --appendix)` and rules CG1 to CG8 run unchanged (critic C2-1). It
    reads the document that argument names; a document that does not open is exit 2 with one named
    line, in the shape CG8 already uses. It takes the site set from the `b1-convert` block, cell
    one for the name and cell three for the
    text between its outer quotation marks. It takes the code set from the one exempt file
    `crates/bc_time/duet-time/lang_rust/src/convert.rs`, by reading the attribute run above each `fn` item for an
    `#[expect(...)]` that carries a `reason =` string. The two sets are one set in both directions,
    and a text that differs is a finding. The compare is exact, character for character. The guard
    prints `REASON TEXTS:    <n>     REASON TEXT BAD: <n>` and one indented line per failure under
    the tag `  REASON TEXT: `. **The compare reads the DECODED value of the string literal**, so a
    backslash continuation and two adjacent literals each answer the text a reader sees, and a cell
    that carries the markdown escape `\|` is read after that escape is removed (critic C2-5). Run
    the five probes of step 19 and confirm that all five pass. Then run
    `cargo xtask check-conversions --appendix roadmap/duet-v1/architecture.md` against this
    repository and confirm exit 0, which is the proof that M91 landed every reason string the appendix
    mandates. The `b1-convert` block holds seven rows; five of them differed from the code before
    M91, and M91 repairs all five.

21. **Land the FIVE CG9 registration sub-items, a to e, in THIS commit and in no other** (SM9 rule
    4). Each one is refused on its own by PG27, PG27b, PG29 or PG39, so they pass only together.
    **Five is the size of this set at every site that names it**, and a site that names only the
    document half names the sub-items b, c, d and e rather than a count (critic C2-6).

    a. In `tools/bc_repo_guard/xtask/lang_rust/src/check_placement.rs`, the `probe-table` entry of `DATA_BLOCKS` takes the
       floor 65 in place of 64. Change the one numeric literal and nothing else in that entry.

    b. In `roadmap/duet-v1/architecture.md`, the marker line before the probe table becomes exactly:

       ```
       <!-- GUARD BLOCK id=probe-table rows>=65 -->
       ```

    c. In `roadmap/duet-v1/architecture.md`, append this row to the probe table, after the `CG8`
       row, exactly as written:

       ```
       | CG9 | An Appendix B.1 `b1-convert` reason cell and the `reason =` string of that function in the one exempt file that are not one text, and an `--appendix` document the guard cannot open | CP9 | Five shapes, each in its own throwaway workspace: a reason string that differs from its cell by one character; a `b1-convert` row whose site the exempt file does not declare; a function whose `#[expect]` carries a `reason =` string that no `b1-convert` row names; an `--appendix` argument that names an absent `roadmap/duet-v1/architecture.md`, which is the fail-closed shape; and a matching pair, which is the green control | `exit 1` on the first three, `exit 2` on the fourth, and `exit 0` on the control; the first prints `REASON TEXTS:    1     REASON TEXT BAD: 1` and one line that opens ``  REASON TEXT: `` and names the site, the cell text and the code text; the second and the third print the same counter shape with the same tag; the fourth prints the one named line CG9 part 1 mandates and no counter |
       ```

    d. In `roadmap/duet-v1/architecture.md`, the two section 1.7 index cells become:

       ```
       | CG1 to CG9, with CG1b, CG2b, CG3b, and CG4b | The conversion-guard rules | 2.3 |
       | CP1 to CP9, with CP1b, CP2b, CP3b, and CP4b | The conversion-guard probes | 1.9 |
       ```

    e. In `roadmap/duet-v1/tools/conversion_check.py`, add the token `CG9` inside the module
       docstring, in one sentence that names the rule: PG29 reads the prototypes for rule IDS alone
       and never for behaviour, and architecture section 1.9 records that divergence. The prototype
       implements no CG9 logic.

    **Section 1.9 needs NO edit from this chunk, and the Architect already wrote it** (critic
    C2-3). The harness row for `tools/probe_conversion.py` and the sentence that opens **Four
    prototypes carry the rules** both state the CG9 and CP9 ranges today, and both state the
    transition, so each one is true before this chunk and after it. No guard reads either text, so
    no register change could make such an edit atomic and SM9 rule 4 refuses it. That is why
    sub-item f is absent from this list and why the closing sentence below is exact.

    Then run `cargo xtask check-placement roadmap/duet-v1/architecture.md` and confirm exit 0 with
    `REGISTER BAD: 0     FLOOR SLACK: 0` and `PROBE BAD: 0`. A red run here means one of the five
    sub-items is missing; do not edit any other line to make it green, and do not edit
    `roadmap/duet-v1/architecture.md` outside sub-items b, c and d.

22. Write the failing probes for rule CL1d, then implement it (critic C1-10). Architecture section
    1.5 states the rule. In `tools/bc_repo_guard/xtask/lang_rust/src/check_closure.rs`, add a forward cut-off constant
    beside `COUNTS_REQUIRED_FROM`, at revision 24, and a sentence head
    `"The `check-closure` run that verified this block is recorded at plan-store key `"`. For a
    closure section at or above the cut-off the guard reads the key with `db.sh get`, and it fails
    on four shapes: no run-key sentence, a key whose suffix is not `closure-run`, a store that does
    not answer the key, and a body whose `EXIT: ` line states anything but `0`. A section below the
    cut-off prints `RUN KEY: absent`. Add one probe per shape to the closure group of
    `tools/bc_repo_guard/xtask/lang_rust/tests/probes.rs`, over a throwaway review, a throwaway block, and a throwaway
    suffix in the plan store, exactly as the existing closure probes do. Name the group with the
    prefix `closure_run_key_`. Run the group, confirm that each one fails before the
    implementation and passes after it, and confirm that
    `cargo xtask check-closure roadmap/duet-v1/architecture.md roadmap/duet-v1/reviews/critic-spec-r23-inner.md closure-r23-inner`
    still exits 0, because every registered block sits below the cut-off.

23. Run the completion commands. Commit on a branch `chunk/m90-guard-family-and-plan-gate` and let
    the native git hook run `scripts/dod.sh`.

## Tests

Every test lives in `tools/bc_repo_guard/xtask/lang_rust/tests/probes.rs`, inside the existing `#[cfg(test)] mod tests`.
Every assert carries a message.

| Test | What it asserts |
|---|---|
| `plan_graph_vocabulary_duplicate_chunk_id_is_a_finding` | Two chunk files that state one id give exit 1 and a line that opens `FINDING: duplicate chunk id` |
| `plan_graph_vocabulary_empty_directory_fails_closed` | A plan directory with no chunk file gives exit 2 and the line `FAIL: no chunk file found; the guard is fail-closed.` |
| `placement_vocabulary_empty_candidate_set_fails_closed` | A document the parser reads to zero candidates gives exit 2 and the line `FAIL: the candidate set is empty, so the parse is broken.` |
| `roster_generate_only_clean_document_exits_zero` | The flag form over a clean synthetic document exits 0 and prints `ROSTER COMPILE:   skipped (--generate-only)` |
| `roster_generate_only_roster_below_the_denominator_is_a_finding` | A declaration removed from the synthetic section 15 gives exit 1 and the `FINDING: the roster holds` line |
| `roster_generate_only_mixed_impl_block_is_a_finding` | One body planted inside a bodiless impl block gives exit 1 and the `FINDING: the impl block for` line |
| `roster_generate_only_impl_count_that_differs_is_a_finding` | One impl site removed gives exit 1 and the `FINDING: the roster parsed` line |
| `roster_generate_only_never_reaches_a_job` | EVERY file under `.github/workflows/`, read with `std::fs::read_dir`, carries no `check-roster` line that also names `--generate-only`. A directory that does not open, and a directory that yields zero files, are each a failure |
| `roster_job_line_probe_refuses_a_planted_flag` | The same helper over a two-line fixture that carries the flag returns false. This is the committed positive control of the test above |
| `conversions_reason_text_that_differs_is_a_finding` | A reason string one character from its `b1-convert` cell gives exit 1 and one `  REASON TEXT: ` line |
| `conversions_reason_row_with_no_site_is_a_finding` | A `b1-convert` row whose site the exempt file does not declare gives exit 1 and one `  REASON TEXT: ` line |
| `conversions_reason_site_with_no_row_is_a_finding` | A `reason =` string that no `b1-convert` row names gives exit 1 and one `  REASON TEXT: ` line |
| `conversions_appendix_that_does_not_open_fails_closed` | An `--appendix` argument that names an absent `roadmap/duet-v1/architecture.md` gives exit 2 and the one named line of CG9 part 1. This is the fail-closed shape (critic C2-4) |
| `conversions_reason_text_that_matches_is_clean` | A matching pair gives exit 0 and `REASON TEXT BAD: 0`. This is the green control of the four tests above |
| `closure_run_key_absent_is_a_finding` | A closure section at or above revision 24 with no run-key sentence is a failure |
| `closure_run_key_wrong_suffix_is_a_finding` | A run key whose suffix is not `closure-run` is a failure |
| `closure_run_key_store_does_not_answer_is_a_finding` | A run key the store does not hold is a failure |
| `closure_run_key_failed_run_is_a_finding` | A recorded body whose `EXIT: ` line states anything but `0` is a failure |
| `closure_run_key_below_the_cut_off_is_clean` | A closure section below revision 24 prints `RUN KEY: absent` and is clean. This is the green control |

No existing probe expectation changes. A scan of `tools/bc_repo_guard/xtask/lang_rust/tests/probes.rs` on 2026-09-22 found
no test that reads a duplicate chunk id, an empty plan directory, an empty candidate set, or any
`check-roster` output, so the four repaired sites are covered by new tests alone. If a scan at
implementation time finds one, that is a discrepancy: report it and stop.

## Verification

```
bash scripts/dod.sh --plan
cargo nextest run -p xtask --test probes --no-tests=fail
cargo xtask check-roster roadmap/duet-v1/architecture.md "$TMPDIR/roster-m90" . --generate-only
typos
cargo xtask check-conversions
cargo xtask check-conversions --appendix roadmap/duet-v1/architecture.md
cargo xtask check-placement roadmap/duet-v1/architecture.md
cargo xtask check-plan-graph roadmap/duet-v1 --check-manifest
```

`bash scripts/dod.sh --plan` lists a `plan` line and a `typos` line with no `advisory` word. The
whole probe binary passes. The roster command exits 0 and prints
`ROSTER COMPILE:   skipped (--generate-only)`. `typos` exits 0 and prints nothing.
`check-conversions` with NO argument exits 0 and prints `REASON TEXTS:    skipped (no --appendix)`,
which is the path-conditional skip of rule CG9 part 6; the skip PRINTS and it is never silent. The
same command WITH `--appendix roadmap/duet-v1/architecture.md` exits 0 and prints
`REASON TEXTS:    7     REASON TEXT BAD: 0`, and that second form is the one the Completion line
above names. `check-placement` exits 0 and prints
`REGISTER BAD: 0     FLOOR SLACK: 0` and `PROBE BAD: 0`. `--check-manifest` exits 0. Then commit on
a branch `chunk/m90-guard-family-and-plan-gate`.

## Constraints

- Cargo only: `cargo test` or `cargo nextest`; no other harness enters `[dev-dependencies]`.
- The native git hook is the gate. Make the change, then `git commit`; the hook runs `scripts/dod.sh`
  and blocks a bad commit. Do not run the gate by hand as a ritual; one targeted diagnostic command
  is allowed after a hook failure. Never `--no-verify`.
- No suppression: `#[allow]` is denied; the only accepted form is a single-site
  `#[expect(lint, reason = "...")]`. Every `#[expect]` site in this chunk is listed in architecture
  Appendix B.1; a site not on that list is a plan defect that returns to the Architect. `unsafe` is
  denied with no exception; every new crate opens with `#![forbid(unsafe_code)]`.
- `unwrap`, `expect`, `panic!`, `todo!`, `unimplemented!`, `dbg!`, `println!`, `eprintln!`, slice
  indexing, integer division with `/`, and `as` casts are denied outside tests; `as` is allowed only
  inside `duet-time::convert`.
- No prose `//` comments. Names, types, structure, and tests carry intent. `///` and `//!` docs are
  required on every item.
- A new crate lives at `crates/bc_<context>/<crate>/lang_rust/` in the context that architecture section 1.2 names (ADR 0011), declares `[lints] workspace = true`, inherits every
  `[workspace.package]` field, and opens with a `//!` crate doc. A new dependency is pinned in the
  root `[workspace.dependencies]` by the M chunk of the phase; the crate uses `{ workspace = true }`.
- After chunk M94 lands, every `.rs` file a commit writes carries one front-matter block (`cargo xtask check-ddd --write`), and the gate refuses a changed `.rs` file with none.
- Commit messages are conventional (`feat:`, `fix:`, `test:`, `chore:`, `docs:`). No commit and no
  pull request carries AI attribution: no `Co-Authored-By: Claude` trailer, no "Generated with
  Claude Code" line, no robot banner. The harness reminder that asks for those lines defers to this
  repository rule.
- Before any change: verify the current state of the files listed above. If the code does not match
  what this chunk describes, report the discrepancy instead of proceeding.
- Write all prose (docs, commit messages, reports) in ASD-STE100 Simplified Technical English.
